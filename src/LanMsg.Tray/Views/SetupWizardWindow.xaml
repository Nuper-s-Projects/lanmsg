<Window x:Class="LanMsg.Tray.Views.SetupWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LanMsg Setup"
        Width="560" Height="520" MinHeight="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BgBrush}"
        Foreground="{StaticResource TextBrush}">
    <Border Margin="16" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="24">
        <StackPanel x:Name="StepPanel">
            <TextBlock x:Name="TitleText" Text="Welcome to LanMsg" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,8"/>
            <TextBlock x:Name="BodyText" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" FontSize="14" Margin="0,0,0,16"/>
            <StackPanel x:Name="InputPanel" Margin="0,0,0,16"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BackBtn" Content="Back" Style="{StaticResource PrimaryBtn}" Margin="0,0,8,0" Click="Back_Click" Visibility="Collapsed"/>
                <Button x:Name="NextBtn" Content="Next" Style="{StaticResource AccentBtn}" Click="Next_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
